Energy Middleware
=================

The `EnergyMiddleware` tracks token usage, estimates energy consumption,
and calculates CO2 emissions for agent calls.

.. autoclass:: jamanota.middleware.EnergyMiddleware
   :members:
   :show-inheritance: